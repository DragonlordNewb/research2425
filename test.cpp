#include "sxl-c++/headers/einstein.h"
#include "sxl-c++/headers/geometry.h"

DECLARE_FUNCTION_3P(H)
REGISTER_FUNCTION(H, dummy(), dummy(), dummy())

int main() {
	geometry::CoordinateSystem coords = {"t", "x", "y", "z"}
	Symbol c("c");
	Expression f = 

	geometry::MetricTensor metric({

	})
}